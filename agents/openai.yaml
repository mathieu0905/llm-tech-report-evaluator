interface:
  display_name: "Paper Review Evaluator"
  short_description: "Stable, review-calibrated paper scoring"
  default_prompt: "Use $paper-review-evaluator to compare, calibrate, and rank these research papers with stable anchors."
